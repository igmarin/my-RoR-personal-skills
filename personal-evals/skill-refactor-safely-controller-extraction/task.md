The `OrdersController#create` action is too fat. Refactor this controller by extracting the core business logic into a service object. You MUST preserve the exact existing behavior. Follow the `refactor-safely` skill: write characterization tests first, verify they pass, perform the extraction in small steps, and verify the tests stay green. Ensure the extracted service follows the Single Responsibility Principle and includes YARD documentation.
