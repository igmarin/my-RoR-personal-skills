{
  "target_skill": "integrate-api-client",
  "source_path": "skills/api/integrate-api-client/SKILL.md",
  "instructions": [
    {
      "instruction": "EVERY layer (Auth, Client, Fetcher, Builder, Entity) MUST have its test",
      "original_snippets": "EVERY layer (Auth, Client, Fetcher, Builder, Entity) MUST have its test",
      "relevant_when": "When a user asks for Rails work that should trigger integrate-api-client.",
      "why_given": "preference"
    },
    {
      "instruction": "Write the spec (instance_double for unit, hash factories for API responses)",
      "original_snippets": "Write the spec (instance_double for unit, hash factories for API responses)",
      "relevant_when": "When a user asks for Rails work that should trigger integrate-api-client.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Run the spec — verify it fails because the layer does not exist yet",
      "original_snippets": "Run the spec — verify it fails because the layer does not exist yet",
      "relevant_when": "When a user asks for Rails work that should trigger integrate-api-client.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Create a FactoryBot hash factory in `spec/factories/module_name/` (use `skip_create` + `initialize_with` — see [LAYERS.md §6](./LAYERS.md) for the pattern)",
      "original_snippets": "Create a FactoryBot hash factory in `spec/factories/module_name/` (use `skip_create` + `initialize_with` — see [LAYERS.md §6](./LAYERS.md) for the pattern)",
      "relevant_when": "When a user asks for Rails work that should trigger integrate-api-client.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Write spec in `spec/services/module_name/` covering `.fetcher`, `.find`/`.search`",
      "original_snippets": "Write spec in `spec/services/module_name/` covering `.fetcher`, `.find`/`.search`",
      "relevant_when": "When a user asks for Rails work that should trigger integrate-api-client.",
      "why_given": "new knowledge"
    }
  ]
}
