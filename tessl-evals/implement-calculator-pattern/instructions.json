{
  "target_skill": "implement-calculator-pattern",
  "source_path": "skills/patterns/implement-calculator-pattern/SKILL.md",
  "instructions": [
    {
      "instruction": "Write the spec — contexts per variant, plus the NullService path",
      "original_snippets": "Write the spec — contexts per variant, plus the NullService path",
      "relevant_when": "When a user asks for Rails work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Run it — verify it fails because the component does not exist yet",
      "original_snippets": "Run it — verify it fails because the component does not exist yet",
      "relevant_when": "When a user asks for Rails work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Run again — confirm green, then proceed to the next component",
      "original_snippets": "Run again — confirm green, then proceed to the next component",
      "relevant_when": "When a user asks for Rails work that should trigger implement-calculator-pattern.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "raise NotImplementedError, \"#{self.class}#compute_result must be implemented\"",
      "original_snippets": "raise NotImplementedError, \"#{self.class}#compute_result must be implemented\"",
      "relevant_when": "When a user asks for Rails work that should trigger implement-calculator-pattern.",
      "why_given": "preference"
    },
    {
      "instruction": "Always call `super` in `should_calculate?` to preserve the base guard.",
      "original_snippets": "Always call `super` in `should_calculate?` to preserve the base guard.",
      "relevant_when": "When a user asks for Rails work that should trigger implement-calculator-pattern.",
      "why_given": "reminder"
    },
    {
      "instruction": "Each spec suite must cover: inactive plan, nil plan, each named variant, and unknown variant. Mirror the same context structure across all concrete services.",
      "original_snippets": "Each spec suite must cover: inactive plan, nil plan, each named variant, and unknown variant. Mirror the same context structure across all concrete services.",
      "relevant_when": "When a user asks for Rails work that should trigger implement-calculator-pattern.",
      "why_given": "preference"
    }
  ]
}
