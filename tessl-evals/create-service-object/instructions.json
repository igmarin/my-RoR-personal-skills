{
  "target_skill": "create-service-object",
  "source_path": "skills/patterns/create-service-object/SKILL.md",
  "instructions": [
    {
      "instruction": "EVERY service object MUST have its test written and validated BEFORE implementation.",
      "original_snippets": "EVERY service object MUST have its test written and validated BEFORE implementation.",
      "relevant_when": "When a user asks for Rails work that should trigger create-service-object.",
      "why_given": "preference"
    },
    {
      "instruction": "Write the spec for .call (with contexts for success, error, edge cases)",
      "original_snippets": "Write the spec for .call (with contexts for success, error, edge cases)",
      "relevant_when": "When a user asks for Rails work that should trigger create-service-object.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Run the spec — verify it fails because the service does not exist yet",
      "original_snippets": "Run the spec — verify it fails because the service does not exist yet",
      "relevant_when": "When a user asks for Rails work that should trigger create-service-object.",
      "why_given": "new knowledge"
    },
    {
      "instruction": "`.call` MUST return EXACTLY one of these two shapes — no other top-level keys, never a Boolean, raw model, or ActiveRecord object:",
      "original_snippets": "`.call` MUST return EXACTLY one of these two shapes — no other top-level keys, never a Boolean, raw model, or ActiveRecord object:",
      "relevant_when": "When a user asks for Rails work that should trigger create-service-object.",
      "why_given": "preference"
    },
    {
      "instruction": "When no instance state is needed — use ONLY class methods, no `initialize`, no instance variables. Validators and stateless helpers should always use this pattern:",
      "original_snippets": "When no instance state is needed — use ONLY class methods, no `initialize`, no instance variables. Validators and stateless helpers should always use this pattern:",
      "relevant_when": "When a user asks for Rails work that should trigger create-service-object.",
      "why_given": "reminder"
    }
  ]
}
