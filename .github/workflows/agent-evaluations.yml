name: Agent Skills Evaluation

on:
  pull_request:
    paths:
      - 'skills/**'
      - 'evals/**'
      - 'evaluator/**'
  workflow_dispatch:

jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true
          working-directory: evaluator

#      - name: Run Evaluator
#        env:
#          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
#        run: |
#          # Run evaluations for the changed skills
#          # For now, we run all skills to ensure full coverage as per Phase 3
#          ./evaluator/bin/evaluate -e evals/skills -s skills -o evaluation-report.json
#
#      - name: Upload Report
#        if: always()
#        uses: actions/upload-artifact@v4
#        with:
#          name: evaluation-report
#          path: evaluation-report.json
