.DS_Store
.agents/skills/
.agents/*
# Claude Code internal state (worktrees, plans, session data)
.claude/
# Gold-standard evaluation scenarios are committed
# Temporary or private sandbox runs are ignored
evals/tmp/
private-evals/
tmp/
.Gemfile.lock
.Gemfile
.agents/skills/

# IDE / AI assistant configs (personal, not shared)
.crush/
.codex/
.codex-plugin/
.gemini/
.gemini-plugin/
.cursor/
.windsurf/
.tessl/
.ruby-lsp/
.vscode/

# Plugin manifests (required by CI - shared)
# .cursor-plugin/ and .windsurf-plugin/ are NOT ignored - CI needs these

# Node
node_modules/
.windsurfrules
.markdownlint.yaml

# Ruby package artifacts
*.gem

# Public root-level skill. Override common global build/ ignores.
!build/
!build/SKILL.md
